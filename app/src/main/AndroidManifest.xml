<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        ВАЖНО: без INTERNET любая сеть падает с SocketException EPERM (Operation not permitted),
        из-за чего RecyclerView остаётся пустым (данные не загружаются).
     -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Полезно для диагностики/логики offline/online, но не критично -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/img_3"
        android:label="@string/app_name"
        android:roundIcon="@drawable/img_3"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.RuStore">
        <activity android:name=".ui.theme.OnboardingActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.theme.AppStoreActivity" android:exported="false" />
        <activity android:name=".ui.theme.CategoriesActivity" android:exported="false" />
        <activity android:name=".ui.theme.SearchActivity" android:exported="false" />
        <activity android:name=".ui.theme.AppCardActivity" android:exported="false" />

        <service
            android:name=".ui.theme.service.ApkDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".ui.theme.service.PackageInstallResultReceiver"
            android:exported="false" />

        <activity
            android:name=".ui.theme.service.InstallApkActivity"
            android:exported="false"
            android:theme="@style/Theme.RuStore" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>